<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iot.mqtt.mapper.DataBaseMapper">
    <select id="list" resultType="java.util.Map">
        ${sqlStr}
    </select>
    <select id="exist" resultType="int">
        select count(*) from person_position.card_sender_detail where code_sn=#{code_sn} and card_number=#{card_number}
    </select>

    <insert id="add">
        INSERT INTO person_position.card_sender_detail (code_sn, aims, num, card_number, power, create_dept, create_by, create_time, update_by, update_time)
        VALUES (#{code_sn}, #{card_number}, #{card_number}, #{card_number}, 100, 1001, 2001, '2024-05-10 08:30:00', 2001, '2024-05-10 08:30:00');
    </insert>
</mapper>